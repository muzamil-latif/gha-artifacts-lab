name: Service Container Demo

on: push

jobs:
  service-demo:
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis

    steps:
      - name: Print Message
        run: echo "Redis service container started"