name: Service Container Demo

on: push

jobs:
  service-demo:
    runs-on: ubuntu-latest

    services:
      redis:
        image: redis
        ports:
          - 6379:6379

    steps:
      - name: Check Redis Port
        run: |
          nc -zv localhost 6379