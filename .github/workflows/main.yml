name: Environment Variables Practice

on: push

env:
  APP_NAME: gha-artifacts-lab

jobs:
  show-variable:
    runs-on: ubuntu-latest

    steps:
      - name: Print App Name
        run: echo $APP_NAME